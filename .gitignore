.idea/

# Local environment / secrets — never commit
.env
.env.*
!.env.docker.example
!*.env.template
!*.env.local.template

# Docker
*.local.yml
*.bak-pre-baked-build

# local copy of the prod SQLite snapshot — customer data, never commit
prod-data.db
